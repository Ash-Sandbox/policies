# rust-dev

Policy for Rust language development

## What's Included

- `/tmp`, `~/.cargo`, and `~/.rustup` directory access
- `crates.io`, `docs.rs`, and `rust-lang.org` network access
- `cargo`, `rustup`, and other Rust-related binaries
